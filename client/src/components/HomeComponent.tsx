import React from 'react';

type Props = {};

const Home = (props: Props) => {
  // decide on what to render on home page
  // view all entries? or just the most recent? or just the most recent 5?
  // maybe have different ways to view all of your entries - flipbook

  // todays date + you have or havent uploaded an entry for today

  // TODO: decide if APP is home? figure out router situation, '/' vs '/home'
  return <div>Home</div>;
};

export default Home;
