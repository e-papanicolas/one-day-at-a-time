import React from 'react';

type Props = {};

const NoteForm = (props: Props) => {
  // create gql mutation, run compile
  // create form
  // submit the form should upload to server, then redirect to entry page
  return <div>NoteForm</div>;
};

export default NoteForm;
